{
  "_meta": {
    "id": "SYS_Tool.netlistComparison",
    "kind": "method",
    "name": "netlistComparison",
    "className": "SYS_Tool",
    "sourceFile": "dist/api-types.d.ts",
    "codeSegments": {
      "remarks": [],
      "example": [
        {
          "type": "code",
          "content": "```javascript\n// 1. 取工程内前两张 PCB 作为两份网表的来源\nconst pcbs = await eda.dmt_Pcb.getAllPcbsInfo();\nconst docA = pcbs[0];\nconst docB = pcbs[1];\n\n// 2. 同一文档与自身对比：网表完全一致，返回空数组（无差异）\nconst selfDiff = await eda.sys_Tool.netlistComparison(docA.uuid, docA.uuid);\nconsole.log('自身对比差异条数：', selfDiff.length);\n\n// 3. 两张不同 PCB 对比：返回差异清单\nconst diff = await eda.sys_Tool.netlistComparison(docA.uuid, docB.uuid);\nconsole.log('两文档对比差异条数：', diff.length);\n\n// 4. 展示差异结构：type 为差异类型，object 为差异对象，\n//    net1 / net2 分别是该对象在两份网表中的名称列表（运行时字段名）\nfor (const item of diff.slice(0, 5)) {\n  console.log('差异：', item.type, item.object, '网表 1：', item.net1.join('、') || '（无）', '网表 2：', item.net2.join('、') || '（无）');\n}\n```"
        }
      ]
    }
  },
  "_source": {
    "title": "网表对比",
    "params.netlist1": "网表 1，可以为：①当前工程内的原理图、PCB 的 UUID；②其它工程的工程 UUID 和原理图、PCB UUID；③原理图、PCB 文件数据",
    "params.netlist2": "网表 2，可以为：①当前工程内的原理图、PCB 的 UUID；②其它工程的工程 UUID 和原理图、PCB UUID；③原理图、PCB 文件数据",
    "returns": "网表对比结果"
  },
  "title": "Netlist comparison",
  "params.netlist1": "Netlist 1, which can be: 1. The UUID of a schematic or PCB in the current project; 2. The project UUID and schematic/PCB UUID of another project; 3. Schematic or PCB file data",
  "params.netlist2": "Netlist 2, which can be: 1. The UUID of a schematic or PCB in the current project; 2. The project UUID and schematic/PCB UUID of another project; 3. Schematic or PCB file data",
  "returns": "Netlist comparison result"
}
