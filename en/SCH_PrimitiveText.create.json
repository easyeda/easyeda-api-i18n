{
  "_meta": {
    "id": "SCH_PrimitiveText.create",
    "kind": "method",
    "name": "create",
    "className": "SCH_PrimitiveText",
    "sourceFile": "dist/api-types.d.ts",
    "codeSegments": {
      "remarks": [],
      "example": [
        {
          "type": "code",
          "content": "```javascript\n// 1. 生成随机坐标，避免与画布上已有的文本重合（SCH 坐标单位 10mil）\nconst x = 2000 + Math.floor(Math.random() * 8000);\nconst y = 2000 + Math.floor(Math.random() * 8000);\n\n// 2. 创建一段完整样式的文本：旋转 90 度、红色、Arial 字体、字号 20、加粗、带下划线、居中对齐（CENTER = 5）\nconst text = await eda.sch_PrimitiveText.create(\n  x,\n  y,\n  '嘉立创示例_设计说明',\n  90,\n  '#FF0000',\n  'Arial',\n  20,\n  true,\n  false,\n  true,\n  5\n);\n\n// 3. 创建类保留现场，不删除图元；读回各属性确认样式已生效\nconsole.log('primitiveId:', text.getState_PrimitiveId());\nconsole.log('primitiveType:', text.getState_PrimitiveType());\nconsole.log('position:', text.getState_X(), text.getState_Y());\nconsole.log('content:', text.getState_Content());\nconsole.log('rotation:', text.getState_Rotation());\nconsole.log('textColor:', text.getState_TextColor());\nconsole.log('fontName:', text.getState_FontName());\nconsole.log('fontSize:', text.getState_FontSize());\nconsole.log('bold:', text.getState_Bold());\nconsole.log('italic:', text.getState_Italic());\nconsole.log('underLine:', text.getState_UnderLine());\nconsole.log('alignMode:', text.getState_AlignMode());\n```"
        }
      ]
    }
  },
  "_source": {
    "title": "创建文本",
    "params.x": "坐标 X",
    "params.y": "坐标 Y",
    "params.content": "文本内容",
    "params.rotation": "旋转角度，可选 `0` `90` `180` `270`",
    "params.textColor": "文本颜色，`null` 表示默认",
    "params.fontName": "字体名称，`null` 表示默认",
    "params.fontSize": "字体大小，`null` 表示默认",
    "params.bold": "是否加粗",
    "params.italic": "是否斜体",
    "params.underLine": "是否加下划线",
    "params.alignMode": "对齐模式",
    "returns": "文本图元对象"
  },
  "title": "Create Text",
  "params.x": "X coordinate",
  "params.y": "Y coordinate",
  "params.content": "Text content",
  "params.rotation": "Rotation angle. Options: `0` `90` `180` `270`",
  "params.textColor": "Text color, `null` indicates the default",
  "params.fontName": "Font name, `null` indicates the default",
  "params.fontSize": "Font size. `null` indicates the default",
  "params.bold": "Whether it is bold",
  "params.italic": "Whether it is italic",
  "params.underLine": "Whether it is underlined",
  "params.alignMode": "Alignment mode",
  "returns": "Text primitive object"
}
