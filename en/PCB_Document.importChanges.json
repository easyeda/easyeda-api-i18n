{
  "_meta": {
    "id": "PCB_Document.importChanges",
    "kind": "method",
    "name": "importChanges",
    "className": "PCB_Document",
    "sourceFile": "dist/api-types.d.ts",
    "codeSegments": {
      "remarks": [],
      "example": [
        {
          "type": "code",
          "content": "```javascript\n// 1. 创建测试原理图和测试 PCB\nconst schUuid = await eda.dmt_Schematic.createSchematic();\nawait new Promise(r => setTimeout(r, 1500));\nconst pcbUuid = await eda.dmt_Pcb.createPcb();\nawait new Promise(r => setTimeout(r, 1500));\n\n// 2. 创建板子把原理图与 PCB 关联起来（返回板子名称）\nconst boardName = await eda.dmt_Board.createBoard(schUuid, pcbUuid);\nawait new Promise(r => setTimeout(r, 1500));\nconsole.log('boardName:', boardName);\n\n// 3. 打开 PCB 并从关联原理图导入变更\nawait eda.dmt_EditorControl.openDocument(pcbUuid);\nawait new Promise(r => setTimeout(r, 1000));\nconst imported = await eda.pcb_Document.importChanges();\nconsole.log('imported:', imported);\n\n// 4. 清理：删板子、删 PCB、删原理图\nawait eda.dmt_Board.deleteBoard(boardName);\nawait new Promise(r => setTimeout(r, 1000));\nawait eda.dmt_Pcb.deletePcb(pcbUuid);\nawait eda.dmt_Schematic.deleteSchematic(schUuid);\n```"
        }
      ]
    }
  },
  "_source": {
    "title": "从原理图导入变更",
    "params.uuid": "原理图 UUID，默认为关联在同一个 Board 下的原理图",
    "returns": "导入操作是否成功，导入失败或未传入原理图 UUID 的游离 PCB 将返回 `false`"
  },
  "title": "Import changes from the schematic",
  "params.uuid": "Schematic UUID. By default, the schematic associated with the same Board is used",
  "returns": "Whether the import operation was successful. If the import fails or the free PCB has no schematic UUID passed in, `false` is returned"
}
