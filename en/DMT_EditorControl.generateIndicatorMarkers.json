{
  "_meta": {
    "id": "DMT_EditorControl.generateIndicatorMarkers",
    "kind": "method",
    "name": "generateIndicatorMarkers",
    "className": "DMT_EditorControl",
    "sourceFile": "dist/api-types.d.ts",
    "codeSegments": {
      "remarks": [
        {
          "type": "text",
          "key": "remarks.text.0"
        }
      ],
      "example": [
        {
          "type": "code",
          "content": "```javascript\n// 1. 打开一个原理图页，确定标记作用的画布\nconst pages = await eda.dmt_Schematic.getAllSchematicPagesInfo();\nconst tabId = await eda.dmt_EditorControl.openDocument(pages[0].uuid);\n\n// 2. 生成一组标记：点 + 圆 + 矩形（原理图画布坐标单位 0.01inch）\nconst generated = await eda.dmt_EditorControl.generateIndicatorMarkers(\n  [\n    { type: 'point', x: 100, y: 100 },\n    { type: 'circle', x: 250, y: 150, r: 50 },\n    { type: 'rectangle', left: 350, right: 550, top: 100, bottom: 250 },\n  ],\n  { r: 255, g: 60, b: 60, alpha: 1 }, // 标记颜色\n  2,     // 线宽\n  false, // 不自动缩放定位到标记\n  tabId,\n);\nconsole.log('generated:', generated);\n\n// 3. 用完即清，保持画布干净\nconst removed = await eda.dmt_EditorControl.removeIndicatorMarkers(tabId);\nconsole.log('removed:', removed);\n```"
        }
      ]
    }
  },
  "_source": {
    "title": "生成指示标记",
    "remarks.text.0": "指示标记外形数据中，原理图、符号画布坐标单位跨度为 0.01inch，PCB、封装画布坐标单位跨度为 mil",
    "params.markers": "指示标记外形对象数组",
    "params.color": "指示标记颜色",
    "params.lineWidth": "线宽",
    "params.zoom": "是否定位并缩放",
    "params.tabId": "标签页 ID，如若未传入，则为最后输入焦点的画布",
    "returns": "指示标记生成是否成功，`false` 表示画布不支持该操作或 `tabId` 不存在"
  },
  "title": "Generate indicator markers",
  "remarks.text.0": "In the indicator marker shape data, the coordinate unit span of the schematic and symbol canvases is 0.01inch, and that of the PCB and footprint canvases is mil",
  "params.markers": "Array of indicator marker shape objects",
  "params.color": "Indicator marker color",
  "params.lineWidth": "Line width",
  "params.zoom": "Whether to locate and zoom",
  "params.tabId": "Tab ID. If not passed in, the canvas with the last input focus will be used",
  "returns": "Whether the indicator markers were generated successfully, `false` indicates that the canvas does not support this operation or `tabId` does not exist"
}
