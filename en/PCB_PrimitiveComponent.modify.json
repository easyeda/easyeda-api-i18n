{
  "_meta": {
    "id": "PCB_PrimitiveComponent.modify",
    "kind": "method",
    "name": "modify",
    "className": "PCB_PrimitiveComponent",
    "sourceFile": "dist/api-types.d.ts",
    "codeSegments": {
      "remarks": [],
      "example": [
        {
          "type": "code",
          "content": "```javascript\n// 1. 放置待修改的测试器件（随机坐标避免与画布已有器件重合）：\n//    按立创编号从系统库精确反查器件，返回项不含 libraryUuid，需补上才能用于 create\nconst x = 20000 + Math.floor(Math.random() * 80000);\nconst y = 20000 + Math.floor(Math.random() * 80000);\nconst devices = await eda.lib_Device.searchByProperties({ supplierId: 'C1523' }, undefined, undefined, undefined, 5, 1);\nconst sysLibUuid = await eda.lib_LibrariesList.getSystemLibraryUuid();\nconst comp = await eda.pcb_PrimitiveComponent.create({ libraryUuid: sysLibUuid, uuid: devices[0].uuid }, 1, x, y);\nconst compId = comp.getState_PrimitiveId();\n\n// 2. 读取修改前的位置与旋转角度\nconst beforeX = comp.getState_X();\nconst beforeY = comp.getState_Y();\nconst beforeRotation = comp.getState_Rotation();\n\n// 3. 批量修改：平移 500mil，旋转 0° → 90°\nawait eda.pcb_PrimitiveComponent.modify(compId, { x: beforeX + 500, y: beforeY + 500, rotation: 90 });\n\n// 4. modify 返回后需要重新 get() 才能读到画布上的最新值\nconst refreshed = await eda.pcb_PrimitiveComponent.get(compId);\n\n// 5. 修改类保留现场，供观察修改结果\nconsole.log('primitiveId:', compId);\nconsole.log('x:', beforeX, '→', refreshed.getState_X());\nconsole.log('y:', beforeY, '→', refreshed.getState_Y());\nconsole.log('rotation:', beforeRotation, '→', refreshed.getState_Rotation());\n```"
        }
      ]
    }
  },
  "_source": {
    "title": "修改器件",
    "params.primitiveId": "图元 ID",
    "params.layer": "层",
    "params.x": "坐标 X",
    "params.y": "坐标 Y",
    "params.rotation": "旋转角度",
    "params.primitiveLock": "是否锁定",
    "params.addIntoBom": "是否加入 BOM",
    "params.designator": "位号",
    "params.name": "名称，`null` 表示留空",
    "params.uniqueId": "唯一 ID，`null` 表示留空",
    "params.manufacturer": "制造商，`null` 表示留空",
    "params.manufacturerId": "制造商编号，`null` 表示留空",
    "params.supplier": "供应商，`null` 表示留空",
    "params.supplierId": "供应商编号，`null` 表示留空",
    "returns": "器件图元对象"
  },
  "title": "Modify Device",
  "params.primitiveId": "Primitive ID",
  "params.layer": "Layer",
  "params.x": "X coordinate",
  "params.y": "Y coordinate",
  "params.rotation": "Rotation angle",
  "params.primitiveLock": "Whether it is locked",
  "params.addIntoBom": "Whether Add to BOM",
  "params.designator": "Designator",
  "params.name": "Name, `null` leave it empty",
  "params.uniqueId": "Unique ID, `null` leave it empty",
  "params.manufacturer": "Manufacturer, `null` leave it empty",
  "params.manufacturerId": "Manufacturer ID, `null` leave it empty",
  "params.supplier": "Supplier, `null` leave it empty",
  "params.supplierId": "Supplier ID, `null` leave it empty",
  "returns": "Device primitive object"
}
