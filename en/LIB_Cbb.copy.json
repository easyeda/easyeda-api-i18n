{
  "_meta": {
    "id": "LIB_Cbb.copy",
    "kind": "method",
    "name": "copy",
    "className": "LIB_Cbb",
    "sourceFile": "dist/api-types.d.ts",
    "codeSegments": {
      "remarks": [],
      "example": [
        {
          "type": "code",
          "content": "```javascript\n// 1. 获取系统库与个人库 UUID\nconst systemLib = await eda.lib_LibrariesList.getSystemLibraryUuid();\nconst personalLib = await eda.lib_LibrariesList.getPersonalLibraryUuid();\n\n// 2. 从系统库找一个复用模块作为复制来源\nconst results = await eda.lib_Cbb.search('', systemLib, undefined, 1);\nconst source = results[0];\n\n// 3. 复制到个人库，指定新名称避免同名冲突（分类传 [] = 不分类）\nconst newName = '嘉立创示例_复制_' + Date.now();\nconst copiedUuid = await eda.lib_Cbb.copy(source.uuid, systemLib, personalLib, [], newName);\n\n// 创建类保留现场（复制品留在个人库中供观察）\n\nconsole.log('source:', source.name, source.uuid);\nconsole.log('copiedUuid:', copiedUuid);\nconsole.log('newName:', newName);\n```"
        }
      ]
    }
  },
  "_source": {
    "title": "复制复用模块",
    "params.cbbUuid": "复用模块 UUID",
    "params.libraryUuid": "库 UUID，可以使用 {@link LIB_LibrariesList} 内的接口获取",
    "params.targetLibraryUuid": "目标库 UUID",
    "params.targetClassification": "目标库内的分类",
    "params.newCbbName": "新复用模块名称，如若目标库内存在重名复用模块将导致复制失败",
    "returns": "目标库内新复用模块的 UUID"
  },
  "title": "Copy Reuse block",
  "params.cbbUuid": "Reuse block UUID",
  "params.libraryUuid": "Library UUID, you can use {@link LIB_LibrariesList} APIs in",
  "params.targetLibraryUuid": "Target library UUID",
  "params.targetClassification": "Classification in the target library",
  "params.newCbbName": "New reuse block name. If a reuse block with the same name exists in the target library, the copy will fail",
  "returns": "UUID of the new reuse block in the target library"
}
