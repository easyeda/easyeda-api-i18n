{
  "_meta": {
    "id": "LIB_Device.copy",
    "kind": "method",
    "name": "copy",
    "className": "LIB_Device",
    "sourceFile": "dist/api-types.d.ts",
    "codeSegments": {
      "remarks": [],
      "example": [
        {
          "type": "code",
          "content": "```javascript\n// 1. 获取个人库 UUID\nconst libraryUuid = await eda.lib_LibrariesList.getPersonalLibraryUuid();\n\n// 2. 新建一个轻量器件作为复制来源\nconst sourceName = '嘉立创示例_复制源_' + Date.now();\nconst sourceUuid = await eda.lib_Device.create(libraryUuid, sourceName, [], { symbolType: 2 });\n\n// 3. 复制到同一库，指定新名称避免同名冲突（分类传 [] = 不分类）\nconst newName = '嘉立创示例_复制品_' + Date.now();\nconst copiedUuid = await eda.lib_Device.copy(sourceUuid, libraryUuid, libraryUuid, [], newName);\n\n// 创建类保留现场（原件与复制品都留在个人库中供观察）\n\nconsole.log('sourceUuid:', sourceUuid);\nconsole.log('copiedUuid:', copiedUuid);\nconsole.log('newName:', newName);\n```"
        }
      ]
    }
  },
  "_source": {
    "title": "复制器件",
    "params.deviceUuid": "器件 UUID",
    "params.libraryUuid": "库 UUID，可以使用 {@link LIB_LibrariesList} 内的接口获取",
    "params.targetLibraryUuid": "目标库 UUID",
    "params.targetClassification": "目标库内的分类",
    "params.newDeviceName": "新器件名称，如若目标库内存在重名器件将导致复制失败",
    "returns": "目标库内新器件的 UUID"
  },
  "title": "Copy Device",
  "params.deviceUuid": "Device UUID",
  "params.libraryUuid": "Library UUID, you can use {@link LIB_LibrariesList} APIs in",
  "params.targetLibraryUuid": "Target library UUID",
  "params.targetClassification": "Classification in the target library",
  "params.newDeviceName": "New device name. If a device with the same name exists in the target library, the copy will fail",
  "returns": "UUID of the new device in the target library"
}
