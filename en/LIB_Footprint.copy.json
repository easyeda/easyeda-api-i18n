{
  "_meta": {
    "id": "LIB_Footprint.copy",
    "kind": "method",
    "name": "copy",
    "className": "LIB_Footprint",
    "sourceFile": "dist/api-types.d.ts",
    "codeSegments": {
      "remarks": [],
      "example": [
        {
          "type": "code",
          "content": "```javascript\n// 1. 搜索系统库，取一个封装作为复制来源\nconst [source] = await eda.lib_Footprint.search('0402', undefined, undefined, 1, 1);\n\n// 2. 获取个人库 UUID\nconst targetLibraryUuid = await eda.lib_LibrariesList.getPersonalLibraryUuid();\n\n// 3. 从系统库复制到个人库，指定新名称避免同名冲突（分类传 [] = 不分类）\nconst newName = '嘉立创示例_封装副本_' + Date.now();\nconst copiedUuid = await eda.lib_Footprint.copy(\n  source.uuid,\n  source.libraryUuid,\n  targetLibraryUuid,\n  [],\n  newName\n);\n\n// 创建类保留现场（副本留在个人库中供观察）\n\nconsole.log('sourceUuid:', source.uuid);\nconsole.log('copiedUuid:', copiedUuid);\nconsole.log('newName:', newName);\n```"
        }
      ]
    }
  },
  "_source": {
    "title": "复制封装",
    "params.footprintUuid": "封装 UUID",
    "params.libraryUuid": "库 UUID，可以使用 {@link LIB_LibrariesList} 内的接口获取",
    "params.targetLibraryUuid": "目标库 UUID",
    "params.targetClassification": "目标库内的分类",
    "params.newFootprintName": "新封装名称，如若目标库内存在重名封装将导致复制失败",
    "returns": "目标库内新封装的 UUID"
  },
  "title": "Copy Footprint",
  "params.footprintUuid": "Footprint UUID",
  "params.libraryUuid": "Library UUID, you can use {@link LIB_LibrariesList} APIs in",
  "params.targetLibraryUuid": "Target library UUID",
  "params.targetClassification": "Classification in the target library",
  "params.newFootprintName": "New footprint name. If a footprint with the same name exists in the target library, the copy will fail",
  "returns": "UUID of the new footprint in the target library"
}
