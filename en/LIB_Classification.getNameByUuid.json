{
  "_meta": {
    "id": "LIB_Classification.getNameByUuid",
    "kind": "method",
    "name": "getNameByUuid",
    "className": "LIB_Classification",
    "sourceFile": "dist/api-types.d.ts",
    "codeSegments": {
      "remarks": [],
      "example": [
        {
          "type": "code",
          "content": "```javascript\n// 1. 获取个人库 UUID\nconst libraryUuid = await eda.lib_LibrariesList.getPersonalLibraryUuid();\n\n// 2. 创建两级测试分类（复用模块库，ELIB_LibraryType.CBB = '1'）\nconst tag = Date.now();\nconst primaryName = '嘉立创示例_按UUID查名一级_' + tag;\nconst secondaryName = '嘉立创示例_按UUID查名二级_' + tag;\nconst primaryIndex = await eda.lib_Classification.createPrimary(libraryUuid, '1', primaryName);\nconst secondaryIndex = await eda.lib_Classification.createSecondary(\n  libraryUuid, '1', primaryIndex.primaryClassificationUuid, secondaryName\n);\n\n// 3. 只传一级 UUID：返回一级名称\nconst primaryNames = await eda.lib_Classification.getNameByUuid(\n  libraryUuid, '1', primaryIndex.primaryClassificationUuid\n);\n\n// 4. 传一级 + 二级 UUID：两级名称都返回\nconst bothNames = await eda.lib_Classification.getNameByUuid(\n  libraryUuid, '1',\n  primaryIndex.primaryClassificationUuid,\n  secondaryIndex.secondaryClassificationUuid\n);\n\n// 5. 清理测试分类（查询类需要清理）\nawait eda.lib_Classification.deleteByUuid(libraryUuid, primaryIndex.primaryClassificationUuid);\n\nconsole.log('primaryNames:', primaryNames);\nconsole.log('bothNames:', bothNames);\n```"
        }
      ]
    }
  },
  "_source": {
    "title": "获取指定 UUID 的分类的名称",
    "params.libraryUuid": "库 UUID",
    "params.libraryType": "库类型",
    "params.primaryClassificationUuid": "一级分类 UUID",
    "params.secondaryClassificationUuid": "二级分类 UUID，如若不指定，则只获取一级分类的信息",
    "returns": "两级分类的名称",
    "deprecated": "since EDA v3.2; dropped EDA v3.3"
  },
  "title": "Get the name of the classification with the specified UUID",
  "params.libraryUuid": "Library UUID",
  "params.libraryType": "Library type",
  "params.primaryClassificationUuid": "Primary classification UUID",
  "params.secondaryClassificationUuid": "Secondary classification UUID. If not specified, only the primary classification information is obtained",
  "returns": "Name of the two-level classification",
  "deprecated": "since EDA v3.2; dropped EDA v3.3"
}
