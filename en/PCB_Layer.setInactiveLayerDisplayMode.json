{
  "_meta": {
    "id": "PCB_Layer.setInactiveLayerDisplayMode",
    "kind": "method",
    "name": "setInactiveLayerDisplayMode",
    "className": "PCB_Layer",
    "sourceFile": "dist/api-types.d.ts",
    "codeSegments": {
      "remarks": [],
      "example": [
        {
          "type": "code",
          "content": "```javascript\n// 1. 将非激活层设为置灰模式（TURN_GRAY=1）\nconst setResult = await eda.pcb_Layer.setInactiveLayerDisplayMode(1);\n\n// 2. 恢复为正常亮度（NORMAL_BRIGHTNESS=0），避免影响日常查看\nconst restoreResult = await eda.pcb_Layer.setInactiveLayerDisplayMode(0);\n\nconsole.log('setResult:', setResult);\nconsole.log('restoreResult:', restoreResult);\n```"
        }
      ]
    }
  },
  "_source": {
    "title": "设置非激活层展示模式",
    "params.displayMode": "展示模式",
    "returns": "是否设置成功"
  },
  "title": "Set Inactive layer display mode",
  "params.displayMode": "Display mode",
  "returns": "Whether Set Successful"
}
