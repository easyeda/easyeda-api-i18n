{
  "_meta": {
    "id": "SYS_Dialog.showSelectDialog.2",
    "kind": "method",
    "name": "showSelectDialog",
    "className": "SYS_Dialog",
    "sourceFile": "dist/api-types.d.ts"
  },
  "_source": {
    "title": "弹出多选窗口",
    "params.options": "选项列表，可以为字符串数组或对象数组，在未指定 `defaultOption` 时，默认值为列表的第一项； 如若为字符串数组，则选项的值和选项的展示内容将保持一致； 如若为对象数组，则 `value` 表示选项的值，`displayContent` 表示选项的展示内容",
    "params.beforeContent": "多选框上方文字",
    "params.afterContent": "多选框下方文字",
    "params.title": "多选框标题",
    "params.defaultOption": "默认选项数组，以选项的值作为匹配参数",
    "params.multiple": "是否支持多选",
    "params.callbackFn": "回调函数",
    "returns": "用户选择的值的集合数组，对应传入的 `options` 中的 `value` 字段"
  },
  "title": "Show a multi-select dialog",
  "params.options": "Option list, which can be an array of strings or an array of objects. When `defaultOption` is not specified, the default value is the first item of the list. If it is an array of strings, the value and display content of the option remain the same. If it is an array of objects, `value` represents the value of the option, and `displayContent` represents the display content of the option",
  "params.beforeContent": "Text above the multi-select box",
  "params.afterContent": "Text below the multi-select box",
  "params.title": "Multi-select box title",
  "params.defaultOption": "Default option array, using the option values as matching parameters",
  "params.multiple": "Whether multiple selection is supported",
  "params.callbackFn": "Callback function",
  "returns": "Array of the set of values selected by the user, corresponding to the `value` fields in the passed-in `options`"
}
