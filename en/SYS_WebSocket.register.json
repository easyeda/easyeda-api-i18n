{
  "_meta": {
    "id": "SYS_WebSocket.register",
    "kind": "method",
    "name": "register",
    "className": "SYS_WebSocket",
    "sourceFile": "dist/api-types.d.ts",
    "codeSegments": {
      "remarks": [
        {
          "type": "text",
          "key": "remarks.text.0"
        },
        {
          "type": "text",
          "key": "remarks.text.1"
        }
      ],
      "example": [
        {
          "type": "code",
          "content": "```javascript\n// 1. 注册连接，挂上「连接成功」与「收到消息」两个回调\neda.sys_WebSocket.register('嘉立创示例_注册', 'ws://127.0.0.1:49620',\n  (event) => {\n    // 服务器推送的每条消息都会进入这个回调（event.data 是消息内容）\n    console.log('收到服务器消息：', event.data);\n  },\n  () => {\n    console.log('连接已建立');\n  });\n\n// 2. register 是同步调用，只登记意图，握手需要一点时间，等待回调触发\nawait new Promise(r => setTimeout(r, 1500));\n\n// 3. 演示完毕后关闭连接，避免残留\neda.sys_WebSocket.close('嘉立创示例_注册');\nconsole.log('已关闭连接');\n```"
        }
      ]
    }
  },
  "_source": {
    "title": "注册 WebSocket 连接",
    "remarks.text.0": "可以用来执行前检测 WebSocket 连接是否正常，但需要注意 **不要尝试相同 ID 不同参数的连接**，这会造成混乱：\n如果存在指定 ID 且处于活跃状态中的 WebSocket 连接，那么其余参数的变更将不会被应用",
    "remarks.text.1": "注意：本接口需要使用者启用扩展的外部交互权限，如若未启用将始终 `throw Error`",
    "params.id": "自定义 WebSocket ID",
    "params.serviceUri": "WebSocket 服务地址",
    "params.receiveMessageCallFn": "接收到消息时的回调函数",
    "params.connectedCallFn": "连接建立时的回调函数",
    "params.protocols": "子协议"
  },
  "title": "Register a WebSocket connection",
  "remarks.text.0": "It can be used to detect whether the WebSocket connection is normal before execution, but note that **do not attempt connections with the same ID but different parameters**, as this will cause confusion:\nif a WebSocket connection with the specified ID is active, changes to other parameters will not be applied",
  "remarks.text.1": "Note: This API requires the user to enable the extension external interaction permission, if not enabled, it will always `throw Error`",
  "params.id": "Custom WebSocket ID",
  "params.serviceUri": "WebSocket service URI",
  "params.receiveMessageCallFn": "Callback function when a message is received",
  "params.connectedCallFn": "Callback function when the connection is established",
  "params.protocols": "Sub-protocols"
}
