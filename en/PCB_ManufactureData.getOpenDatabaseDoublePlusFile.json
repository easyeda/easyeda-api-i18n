{
  "_meta": {
    "id": "PCB_ManufactureData.getOpenDatabaseDoublePlusFile",
    "kind": "method",
    "name": "getOpenDatabaseDoublePlusFile",
    "className": "PCB_ManufactureData",
    "sourceFile": "dist/api-types.d.ts",
    "codeSegments": {
      "remarks": [
        {
          "type": "text",
          "key": "remarks.text.0"
        }
      ],
      "example": [
        {
          "type": "code",
          "content": "```javascript\n// 导出 ODB++ 文件，自定义单位和选项\nconst odbFile = await eda.pcb_ManufactureData.getOpenDatabaseDoublePlusFile(\n    'MyBoard_ODB',\n    ESYS_Unit.INCH,\n    {\n        metallizedDrilledHoles: true,\n        nonMetallizedDrilledHoles: true,\n        drillTable: true,\n        flyingProbeTestFile: false\n    }\n);\nif (odbFile) {\n    await eda.sys_FileSystem.saveFile(odbFile);\n}\n```"
        }
      ]
    }
  },
  "_source": {
    "title": "获取 ODB++ 文件",
    "remarks.text.0": "可以使用 {@link SYS_FileSystem.saveFile} 接口将文件导出到本地文件系统",
    "params.fileName": "文件名",
    "params.unit": "单位",
    "params.otherData": "其它",
    "params.layers": "导出层，默认则按照嘉立创生产需求导出",
    "params.objects": "导出对象，默认则按照嘉立创生产需求导出",
    "returns": "ODB++ 文件数据"
  },
  "title": "Get ODB++ file",
  "remarks.text.0": "You can use {@link SYS_FileSystem.saveFile} API export the file to the local file system",
  "params.fileName": "File name",
  "params.unit": "Unit",
  "params.otherData": "Other",
  "params.layers": "Exported layers. By default, they are exported according to EasyEDA production requirements",
  "params.objects": "Exported objects. By default, they are exported according to EasyEDA production requirements",
  "returns": "ODB++ file data"
}
