{
  "_meta": {
    "id": "DMT_EditorControl.getSplitScreenTree",
    "kind": "method",
    "name": "getSplitScreenTree",
    "className": "DMT_EditorControl",
    "sourceFile": "dist/api-types.d.ts",
    "codeSegments": {
      "remarks": [],
      "example": [
        {
          "type": "code",
          "content": "```javascript\n// 1. 打开一个原理图页，保证分屏树里有标签页可读\nconst pages = await eda.dmt_Schematic.getAllSchematicPagesInfo();\nawait eda.dmt_EditorControl.openDocument(pages[0].uuid);\n\n// 2. 读取分屏树\nconst tree = await eda.dmt_EditorControl.getSplitScreenTree();\n\n// 3. 递归遍历，收集所有分屏 ID 与标签页标题\nconst splitIds = [];\nconst tabTitles = [];\n(function walk(node) {\n  splitIds.push(node.id);\n  if (node.tabs) tabTitles.push(...node.tabs.map(t => t.title));\n  (node.children || []).forEach(walk);\n})(tree);\nconsole.log('split count:', splitIds.length);\nconsole.log('tab titles:', tabTitles);\n```"
        }
      ]
    }
  },
  "_source": {
    "title": "获取编辑器分屏属性树",
    "returns": "编辑器分屏属性树，如若为 `undefined`，则数据获取失败"
  },
  "title": "Get the editor split screen property tree",
  "returns": "The editor split screen property tree. If it is `undefined`, the data retrieval failed"
}
