{
  "_meta": {
    "id": "SYS_WebSocket.send",
    "kind": "method",
    "name": "send",
    "className": "SYS_WebSocket",
    "sourceFile": "dist/api-types.d.ts",
    "codeSegments": {
      "remarks": [
        {
          "type": "text",
          "key": "remarks.text.0"
        }
      ],
      "example": [
        {
          "type": "code",
          "content": "```javascript\n// 1. 注册连接（本例的桥接服务对 ping 消息回 pong，用来演示完整收发回路）\neda.sys_WebSocket.register('嘉立创示例_发送', 'ws://127.0.0.1:49620',\n  (event) => {\n    console.log('收到服务器回复：', event.data);\n  },\n  () => {\n    console.log('连接已建立');\n  });\n\n// 2. 等待握手完成\nawait new Promise(r => setTimeout(r, 1500));\n\n// 3. 发送数据（data 支持字符串、Blob、BufferSource，这里发 JSON 字符串）\neda.sys_WebSocket.send('嘉立创示例_发送', JSON.stringify({ type: 'ping', id: '嘉立创示例_发送' }));\n\n// 4. 等待服务器回复进入 receiveMessageCallFn\nawait new Promise(r => setTimeout(r, 1500));\n\n// 5. 演示完毕后关闭连接\neda.sys_WebSocket.close('嘉立创示例_发送');\nconsole.log('已关闭连接');\n```"
        }
      ]
    }
  },
  "_source": {
    "title": "向 WebSocket 服务器发送数据",
    "remarks.text.0": "注意：本接口需要使用者启用扩展的外部交互权限，如若未启用将始终 `throw Error`",
    "params.id": "自定义的 WebSocket ID",
    "params.data": "发送的数据",
    "params.extensionUuid": "扩展 UUID，一般不需要指定，仅当需要操作其它扩展建立的 WebSocket 连接时才需要指定为其它扩展的 UUID"
  },
  "title": "Send data to the WebSocket server",
  "remarks.text.0": "Note: This API requires the user to enable the extension external interaction permission, if not enabled, it will always `throw Error`",
  "params.id": "Custom WebSocket ID",
  "params.data": "Data to send",
  "params.extensionUuid": "Extension UUID. Generally it does not need to be specified. It only needs to be specified as another extension's UUID when you need to operate on a WebSocket connection established by another extension"
}
