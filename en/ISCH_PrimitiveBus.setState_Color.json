{
  "_meta": {
    "id": "ISCH_PrimitiveBus.setState_Color",
    "kind": "method",
    "name": "setState_Color",
    "className": "ISCH_PrimitiveBus",
    "sourceFile": "dist/api-types.d.ts",
    "codeSegments": {
      "remarks": [],
      "example": [
        {
          "type": "code",
          "content": "```javascript\n// 1. 生成本次运行专用的坐标，避免与之前保留的测试总线重合\nconst x = 2000 + Math.floor(Math.random() * 8000);\nconst y = 2000 + Math.floor(Math.random() * 8000);\n\n// 2. 创建一条红色测试总线（SCH 坐标单位 10mil）\nconst bus = await eda.sch_PrimitiveBus.create('DATA[0..7]', [x, y, x + 400, y], '#FF0000', 6, 1);\n\n// 3. 读取修改前的颜色\nconst before = bus.getState_Color();\n\n// 4. 切换异步模式并改为绿色\nconst asyncBus = bus.toAsync();\nasyncBus.setState_Color('#00AA00');\nawait asyncBus.done();\n\n// 5. 从画布重新读取，确认修改已生效（保留现场供观察）\nconst refetched = await eda.sch_PrimitiveBus.get(bus.getState_PrimitiveId());\n\nconsole.log('color:', before, '→', refetched.getState_Color());\n```"
        }
      ]
    }
  },
  "_source": {
    "title": "设置属性状态：总线颜色",
    "params.color": "总线颜色",
    "returns": "总线图元对象"
  },
  "title": "Set the property state: Bus color",
  "params.color": "Bus color",
  "returns": "Bus primitive object"
}
