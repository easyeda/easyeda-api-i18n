{
  "_meta": {
    "id": "DMT_Folder.modifyFolderDescription",
    "kind": "method",
    "name": "modifyFolderDescription",
    "className": "DMT_Folder",
    "sourceFile": "dist/api-types.d.ts",
    "codeSegments": {
      "remarks": [
        {
          "type": "text",
          "key": "remarks.text.0"
        }
      ],
      "example": [
        {
          "type": "code",
          "content": "```javascript\n// 1. 取当前工程所属团队\nconst projectInfo = await eda.dmt_Project.getCurrentProjectInfo();\nconst teamUuid = projectInfo.teamUuid;\n\n// 2. 创建带初始描述的测试文件夹，等 1.5s 让工作区同步\nconst folderUuid = await eda.dmt_Folder.createFolder('嘉立创示例_改描述', teamUuid, undefined, '嘉立创示例：旧描述');\nawait new Promise(r => setTimeout(r, 1500));\n\n// 3. 修改文件夹描述\nconst modified = await eda.dmt_Folder.modifyFolderDescription(teamUuid, folderUuid, '嘉立创示例：更新后的描述');\n\n// 4. 等 1s 让工作区同步，再回读验证\nawait new Promise(r => setTimeout(r, 1000));\nconst folderInfo = await eda.dmt_Folder.getFolderInfo(teamUuid, folderUuid);\n\nconsole.log('modified:', modified);\nconsole.log('description:', folderInfo?.description);\n```"
        }
      ]
    }
  },
  "_source": {
    "title": "修改文件夹描述",
    "remarks.text.0": "修改文件夹描述需要与工作区系统进行交互，修改操作存在延迟，需要短暂等待后才会呈现效果",
    "params.teamUuid": "团队 UUID",
    "params.folderUuid": "文件夹 UUID",
    "params.description": "文件夹描述，如若为 `undefined` 则清空工程现有描述",
    "returns": "是否修改成功"
  },
  "title": "Modify Folder description",
  "remarks.text.0": "Modifying the folder description requires interaction with the workspace system. The modification is delayed and takes effect only after a short wait",
  "params.teamUuid": "Team UUID",
  "params.folderUuid": "Folder UUID",
  "params.description": "Folder description. If it is `undefined`, the existing project description is cleared",
  "returns": "Whether Modify Successful"
}
